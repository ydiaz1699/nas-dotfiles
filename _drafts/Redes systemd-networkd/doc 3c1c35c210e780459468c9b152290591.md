# doc

- doble
    
    Ah, entendido — entonces es mucho más simple, ignorá todo lo de macvlan/AdGuard. Solo tocás `10-eno1.network` para que el host tenga las dos IPs a la vez:
    
    ```bash
    nano /etc/systemd/network/10-eno1.network
    ```
    
    ```
    [Match]
    Name=eno1
    
    [Network]
    Address=192.168.0.200/24
    Address=192.168.1.200/24
    Gateway=192.168.1.1
    DNS=1.1.1.1
    DNS=8.8.8.8
    DHCP=no
    MACVLAN=macvlan-shim
    ConfigureWithoutCarrier=yes
    ```
    
    Aplicás:
    
    ```bash
    systemctl restart systemd-networkd
    ip addr show eno1
    ```
    
    Deberías ver ambas IPs (`192.168.0.200` y `192.168.1.200`) en la interfaz al mismo tiempo. A partir de ahí, sea cual sea el router que esté enchufado, siempre vas a poder entrar por SSH:
    
    ```
    ssh root@192.168.0.200
    ```
    
    o
    
    ```
    ssh root@192.168.1.200
    ```
    
    según cuál subred esté activa — sin tener que editar nada de nuevo. Docker/AdGuard lo dejamos como está ahora, sin tocar, ese lo resolvés después si hace falta.
    
    ¿Lo aplicás y confirmás que aparecen las dos IPs con `ip addr show eno1`?
    
- 1
    
    Ahí está el problema: tu Debian sigue "pegado" con IP fija `192.168.0.200` en la subred vieja, mientras el router nuevo reparte `192.168.1.X`. Por eso el ping local funciona (mismo cable/switch, resuelve por ARP en capa 2) pero SSH normal (que depende de que ambos estén en la misma subred lógica, con gateway correcto) no.
    
    Como corriste `networkctl status`, asumo que usás **systemd-networkd** para la config de red. Pasos:
    
    1. **Ver el archivo de configuración actual** (para editar el correcto):
    
    ```bash
    ls /etc/systemd/network/
    ```
    
    1. **Editar el archivo `.network` correspondiente** (ejemplo, reemplazá `nombre.network` por el que encuentres):
    
    ```bash
    sudo nano /etc/systemd/network/nombre.network
    ```
    
    Cambiar algo como:
    
    ```
    [Network]
    Address=192.168.0.200/24
    Gateway=192.168.0.1
    ```
    
    por:
    
    ```
    [Network]
    Address=192.168.1.200/24
    Gateway=192.168.1.1
    ```
    
    (Ajustá el `.200` final y el gateway según lo que reparta tu router nuevo — muchos routers usan `.1`, pero confirmalo).
    
    1. **Reiniciar el servicio de red**:
    
    ```bash
    sudo systemctl restart systemd-networkd
    ```
    
    1. **Verificar que tomó la IP nueva**:
    
    ```bash
    networkctl status
    ```
    
    ¿Podés pegarme el output de `ls /etc/systemd/network/` y de `networkctl status` para confirmar el nombre exacto de la interfaz y el archivo antes de que edites algo? Así te doy el comando exacto sin que tengas que adivinar el nombre del archivo.
    
- 2
    
    Ojo: `nombre.network` era un **placeholder** mío — no un archivo real. Si lo pegaste tal cual en el `nano`, por eso te abrió vacío (nano crea un archivo nuevo si no existe).
    
    Necesito que confirmes cuál es tu método real de configuración de red. Corré esto y pegame todo el output:
    
    ```bash
    ls -la /etc/systemd/network/
    cat /etc/network/interfaces 2>/dev/null
    nmcli con show 2>/dev/null
    networkctl status
    ```
    
    Con eso identifico si tu IP estática vive en:
    
    - **systemd-networkd** (archivo `.network` real, con otro nombre)
    - **`/etc/network/interfaces`** (método clásico ifupdown)
    - **NetworkManager** (`nmcli`)
    
    y te doy el comando exacto para cambiar `192.168.0.200` → `192.168.1.X`.
    
- 3
    
    Tu interfaz real es **`eno2`** (no `eno1` como en la guía original). El archivo a tocar es `/etc/systemd/network/10-eno2.network`, y case casi seguro sigue apuntando al router viejo.
    
    **Antes que nada**, poné la red de seguridad (por si algo sale mal, se revierte solo en 3 min):
    
    ```bash
    apt install -y at
    echo "systemctl restart networking" | at now + 3 minutes
    ```
    
    **Después, mostrame el contenido actual** para confirmar antes de editar:
    
    ```bash
    cat /etc/systemd/network/10-eno2.network
    ```
    
    Mientras tanto, dos preguntas rápidas:
    
    1. ¿Sabés qué gateway reparte el router nuevo? (lo normal es `192.168.1.1`, pero confirmalo en la config del router o con `ip route` desde Windows)
    2. ¿Llegaste a correr `systemctl disable --now networking` cuando migraste a networkd, o puede que `/etc/network/interfaces` con `eno1` siga activo en paralelo?
- 4
    
    Perfecto, entonces todo coincide con la guía original: la interfaz es `eno1` y el archivo es `/etc/systemd/network/10-eno1.network`.
    
    Antes de editar, mostrame su contenido actual para confirmar que dice lo que la guía indica:
    
    ```bash
    cat /etc/systemd/network/10-eno1.network
    ```
    
    Y confirmame estas dos cosas para no romper nada:
    
    1. **Gateway real del router nuevo** — ¿lo sabés con certeza (ej. `192.168.1.1`) o hay que verificarlo? Se puede confirmar desde Windows con:
    
    ```
    ipconfig
    ```
    
    (mirá el campo "Puerta de enlace predeterminada")
    
    1. Poné ya la red de seguridad, por las dudas (se revierte sola en 3 min si algo falla):
    
    ```bash
    apt install -y at
    echo "systemctl restart networking" | at now + 3 minutes
    ```
    
    Con el contenido del archivo y el gateway confirmado, te paso el `nano` con los valores exactos para cambiar de `192.168.0.200/24` a la IP que quieras en `192.168.1.X/24`.
    
- 5
    
    Encontré el problema y dos errores de tipeo extra en el archivo (no relacionados al corte de SSH, pero conviene arreglarlos de paso).
    
    **Primero, red de seguridad** (si no la pusiste ya):
    
    ```bash
    apt install -y at
    echo "systemctl restart networking" | at now + 3 minutes
    ```
    
    **Después, editá el archivo:**
    
    ```bash
    nano /etc/systemd/network/10-eno1.network
    ```
    
    Dejalo así (asumiendo que el gateway del router nuevo es `192.168.1.1` — **confirmalo antes**, ver abajo):
    
    ```
    [Match]
    Name=eno1
    
    [Network]
    Address=192.168.1.200/24
    Gateway=192.168.1.1
    DNS=1.1.1.1
    DNS=8.8.8.8
    DHCP=no
    MACVLAN=macvlan-shim
    ConfigureWithoutCarrier=yes
    ```
    
    Cambios hechos:
    
    - `192.168.0.200` → `192.168.1.200` (ajustá el último octeto si querés otra IP, pero mantenela fuera del rango DHCP del router)
    - `192.168.0.1` → `192.168.1.1` (gateway — **confirmá esto viendo "Puerta de enlace predeterminada" con `ipconfig` en Windows**)
    - `DCHP=no` → `DHCP=no` (typo corregido)
    - `macvlan-shin` → `macvlan-shim` (typo corregido, para que coincida con el nombre real del netdev)
    
    **Después de guardar:**
    
    ```bash
    systemctl restart systemd-networkd
    networkctl status
    ip addr show eno1
    ```
    
    Y reconectás por SSH a `ssh usuario@192.168.1.200`.
    
    ¿Confirmás el gateway con el `ipconfig` de Windows antes de que apliques el cambio? Si es distinto a `.1`, avisame y ajusto la línea.
    
- 6
    
    Bien ahí. Ahora el mismo problema se repite un nivel más abajo: el **shim macvlan** y la **red Docker de AdGuard** siguen apuntando al rango viejo. Esto es justo lo que está rompiendo contenedores.
    
    **1. Actualizá el shim** (en el Debian, ya conectado por SSH):
    
    ```bash
    nano /etc/systemd/network/20-macvlan-shim.network
    ```
    
    Dejalo así:
    
    ```
    [Match]
    Name=macvlan-shim
    
    [Network]
    Address=192.168.1.250/32
    
    [Route]
    Destination=192.168.1.201/32
    ```
    
    (Ajustá `.250` y `.201` si querés otras IPs, pero mantenelas libres en el nuevo rango)
    
    **2. Aplicá y verificá:**
    
    ```bash
    systemctl restart systemd-networkd
    ip addr show macvlan-shim
    ```
    
    **3. Actualizá el compose de AdGuard** (mismo cambio de rango):
    
    ```bash
    nano /docker/adguard/compose.yaml
    ```
    
    Cambiar:
    
    ```yaml
    networks:
      macvlan_NET:
        ipv4_address: 192.168.0.201    →   192.168.1.201
    ...
        driver_opts:
          parent: eno1
        ipam:
          config:
            - subnet: 192.168.0.0/24   →   192.168.1.0/24
              gateway: 192.168.0.1     →   192.168.1.1
    ```
    
    **4. Recreá el contenedor y la red** (con `restart` no alcanza, hay que recrear la red macvlan de Docker):
    
    ```bash
    cd /docker/adguard
    docker compose down
    docker network prune -f   # limpia redes macvlan huérfanas del rango viejo
    docker compose up -d
    ```
    
    **5. Verificá:**
    
    ```bash
    ping 192.168.1.201
    curl http://192.168.1.201:3000
    ```
    
    Ahora, sobre los **otros contenedores que fallan**: ¿podés decirme cuáles son y pegarme sus `compose.yaml` (o al menos la sección `networks:`)? Si usan la misma red `macvlan_NET` o tienen IPs/gateways hardcodeados del rango `192.168.0.X`, van a necesitar el mismo tipo de cambio. También sirve si corrés esto para ver todas las redes Docker con subredes fijas:
    
    ```bash
    docker network ls
    docker network inspect macvlan_NET 2>/dev/null | grep -A5 IPAM
    ```
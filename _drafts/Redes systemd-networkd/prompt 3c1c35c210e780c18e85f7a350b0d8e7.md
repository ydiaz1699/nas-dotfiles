# prompt

Acá tenés el prompt para mañana. Guardalo y pegalo junto con la guía (`guia-networkd-adguard.md`) al iniciar la nueva conversación:

---

```
Contexto: Estoy migrando la config de red de mi homelab (Debian + systemd-networkd)
tras un cambio de router, de 192.168.0.1 a 192.168.1.1. Adjunto la guía de referencia
que armamos (guia-networkd-adguard.md).

Estado confirmado hasta ahora:
- SSH YA funciona: /etc/systemd/network/10-eno1.network tiene doble IP
  (Address=192.168.0.200/24 y Address=192.168.1.200/24) con Gateway=192.168.1.1.
  Esto permite conectar por SSH sin importar cuál router esté activo.
- Se corrigieron dos typos que tenía el archivo original: DCHP=no -> DHCP=no,
  y macvlan-shin -> macvlan-shim.

Pendiente de verificar/aplicar (no confirmado si quedó hecho):
- /etc/systemd/network/20-macvlan-shim.network: todavía puede estar con
  Address=192.168.0.250/32 y Destination=192.168.0.201/32 (rango viejo).
  Falta confirmar si se actualizó a 192.168.1.1 o no.
- El compose.yaml de AdGuard (macvlan) también puede seguir con
  ipv4_address/subnet/gateway del rango viejo (192.168.0.X).

Quiero continuar SOLO con la parte de red (host + macvlan-shim), sin entrar
todavía en los servicios Docker individuales que puedan estar fallando
(eso lo maneja aparte, en otra conversación).

Primer paso: pedime el output actual de:
- cat /etc/systemd/network/20-macvlan-shim.network
- ip addr show eno1
- ip addr show macvlan-shim
para confirmar en qué estado quedó todo antes de seguir.
```

---

Con esto, mañana arrancás mostrando el estado real (`cat`/`ip addr`) antes de tocar nada, sin arrastrar los temas de AdGuard/HA/homepage que quedaron abiertos por separado.